# 更新日志

本项目的所有重要变更都记录在这里。

格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循[语义化版本](https://semver.org/lang/zh-CN/)。

这里写的是**用户能感知的变化**。想知道代码层面具体改了哪一行，看
[提交历史](../../commits/main)。

## [未发布]

## [1.0.1] - 2026-09-19

### 修复

- **exe 放在不可写目录时会反复弹错误框。** 如果把 `screenshot_tool.exe` 放进
  `Program Files` 这类位置，之前每次截图都会弹一个「无法创建 shots 目录」的
  对话框，而且图存不下来。现在会自动改用 `图片\Screenshots`，静默完成，不丢图。

## [1.0.0] - 2026-09-19

首个版本。

### 新增

- 全局热键 `Ctrl+Alt+Q` 启动截图；全屏选区覆盖窗，`Esc` 或右键取消
- 截图自动存为 PNG，同时复制到剪贴板
- 贴图置顶：截图后直接钉在屏幕上作为参考图，可拖动、拖拽四角等比例缩放、双击关闭
- 贴图的右键菜单：复制到剪贴板 / 另存为 PNG / 打开截图文件夹 / 关闭
- 托盘菜单：截图 / 打开截图文件夹 / 关闭全部贴图 / 退出
- Explorer 崩溃重启后自动恢复托盘图标

### 说明

- 编译只需要免费的 **Build Tools for Visual Studio 2022**，不需要完整的 Visual Studio
- 附带 GitHub Actions 工作流：推送 `v*` 标签即自动构建并发布带 exe 的 Release
- 构建产物不依赖任何运行库（静态 CRT），下载解压即可运行

### 已知限制

- 多显示器与混合 DPI 的代码路径尚未在真实环境验证（开发机只有单显示器）
- 贴图会持续占用内存，约 `宽 × 高 × 4` 字节每张

[未发布]: ../../compare/v1.0.1...HEAD
[1.0.1]: ../../compare/v1.0.0...v1.0.1
[1.0.0]: ../../releases/tag/v1.0.0
